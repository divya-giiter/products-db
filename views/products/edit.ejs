<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit </title>
</head>
<body>
    <h1>Edit <%= foundProduct.name %> details</h1>
    <form action="/products/<%= foundProduct._id %>?_method=PUT" method="POST">
        <div>
            <label for="name">NAME :</label>
            <input type="text" name="name" placeholder="name" id="name" value="<%= foundProduct.name %>">
        </div> 

        <div>
            <label for="price">PRICE :</label>
            <input type="number" name="price" placeholder="price" id="price" value="<%= foundProduct.price %>">
        </div> 

        <div>
        <label for="category">CATEGORY :</label>
        <select name="category" id="category" >
            <% for(let category of categories){ %>
                <option value="<%= category %>" <%= foundProduct.category=== category ? 'selected' :' '%>><%= category %></option>
            <% } %>
        </select>
        </div>

        <button>SAVE CHANGES</button>
    </form>
    <a href="/products/<%= foundProduct._id %>">Cancel Changes</a>
</body>
</html>